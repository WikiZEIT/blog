---
layout: layouts/base.liquid
---

<main id="main-content" class="project-main">
  <div class="breadcrumbs">
    <nav>
      <a href="{{ '/' | url }}">Strona główna</a>
      <span class="material-symbols-outlined">chevron_right</span>
      <a href="{{ '/' | url }}">Projekty</a>
      <span class="material-symbols-outlined">chevron_right</span>
      <span class="current">{{ title }}</span>
    </nav>
  </div>
  <div class="project-header">
    <div class="project-header-top">
      <span class="tool-icon material-symbols-outlined">{{ icon | default: "code" }}</span>
      {% if tags %}
      <div class="post-card-tags">
        {% for tag in tags %}
        <a class="tag-badge" href="{{ '/tags/' | url }}{{ tag | slugify }}/">{{ tag }}</a>
        {% endfor %}
      </div>
      {% endif %}
    </div>
    <h1 class="project-title">
      <a href="{{ projectUrl }}" target="_blank" rel="noopener">{{ title }}
        <span class="material-symbols-outlined">open_in_new</span>
      </a>
    </h1>
    <p class="project-description">{{ description }}</p>
  </div>
  <div class="page-main body">
    {{ content }}
  </div>
</main>
